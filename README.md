# mert-cemri.github.io

Personal website, served by GitHub Pages at https://mert-cemri.github.io/.

Plain HTML and CSS in `index.html`, no build step. Edit the file and push to `master`.
The CV is built from `mert-resume-overleaf/structed.tex` and copied here as `Mert_Cemri_CV.pdf`.
